import React from 'react';
import './Card.css'


const Card = ({repo}) => {
console.log(repo)

const filteredDistricts = repo.map((element)=> {
  console.log(element)
});
  return(
    <div></div>
  )
}

export default Card;
